// ─────────────────────────────────────────────────────────
// Sentry setup – Mobile (Expo / React Native)
// ─────────────────────────────────────────────────────────
// Initializes Sentry for the Expo mobile app.
// Reads DSN from the EXPO_PUBLIC_SENTRY_DSN environment
// variable (public, must be prefixed with EXPO_PUBLIC_).
// Stub – uncomment & configure when you add the
// @sentry/react-native package to apps/mobile/package.json.
// ─────────────────────────────────────────────────────────

// import * as Sentry from "@sentry/react-native";
//
// Sentry.init({
//   dsn: process.env.EXPO_PUBLIC_SENTRY_DSN,
//   environment: process.env.NODE_ENV ?? "development",
//   tracesSampleRate: process.env.NODE_ENV === "production" ? 0.1 : 1.0,
// });

export {};
